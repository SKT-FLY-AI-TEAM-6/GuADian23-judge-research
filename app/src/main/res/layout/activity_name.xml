<?xml version="1.0" encoding="utf-8"?>
<!--
  01-2 · 이름 입력 (피그마 v2 · 1501:2). 01 계정 만들기와 02 역할 선택 사이.
  피그마 값: 앱바 뒤로 44 x=7 · 제목 「이름 입력」 20 Bold x=52 ·
  큰 제목 「이름을 입력해 주세요」 24 Bold/32 · 라벨 「이름」 14 Bold #525C6E ·
  입력칸 60 · #F5F7FA 채움 r14 · 안쪽 20 · 글자 18 Bold ·
  CTA 위 안내 2줄 14.5 #8E97A6 가운데 · CTA 「이름 저장하고 계속」 56.
  세로 간격(28·34·10)만 반영, CTA는 바닥 고정(Insets가 34 하한 보장) — 01과 같은 규칙
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_screen"
    android:orientation="vertical"
    android:paddingTop="8dp"
    android:paddingBottom="0dp">

    <!-- 앱바: 뒤로 화살표 44(x=7) + 제목 20 Bold (x=52) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="7dp"
        android:paddingEnd="24dp">

        <ImageView
            android:id="@+id/btn_back"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:contentDescription="뒤로"
            android:src="@drawable/ic_back" />

        <TextView
            style="@style/AppBarTitle"
            android:layout_marginStart="1dp"
            android:text="이름 입력" />
    </LinearLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:scrollbars="none">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="24dp"
            android:paddingEnd="24dp">

            <!--
              제목이 한 줄이지만 **두 줄 자리**를 차지한다. 시안은 01과 01-2의
              라벨·입력칸을 둘 다 y=222·250에 두었다 — 이어지는 두 화면에서 입력칸이
              제자리에 있어야 누르던 손이 따라가지 않는다. minLines를 빼면 이 화면만
              라벨이 31 위로 올라간다(실측)
            -->
            <TextView
                style="@style/TitleBig"
                android:layout_width="match_parent"
                android:layout_marginTop="28dp"
                android:minLines="2"
                android:text="이름을 입력해 주세요" />

            <TextView
                style="@style/SectionLabel"
                android:layout_marginTop="34dp"
                android:text="이름" />

            <!-- 입력칸 (시안 1501:17 Field/Phone): #F5F7FA 채움 · r14 · 안쪽 20 · 테두리 없음 -->
            <LinearLayout
                android:id="@+id/box_name"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:layout_marginTop="10dp"
                android:gravity="center_vertical"
                android:orientation="horizontal"
                android:paddingStart="20dp"
                android:paddingEnd="20dp">

                <EditText
                    android:id="@+id/input_name"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:background="@null"
                    android:fontFamily="@font/noto"
                    android:gravity="center_vertical"
                    android:hint="한민수"
                    android:importantForAutofill="no"
                    android:includeFontPadding="false"
                    android:inputType="textPersonName"
                    android:letterSpacing="-0.015"
                    android:maxLength="12"
                    android:textColor="@color/ink"
                    android:textColorHint="@color/ink_muted"
                    android:textSize="18sp"
                    android:textStyle="bold" />
            </LinearLayout>
        </LinearLayout>
    </ScrollView>

    <!-- 시안 1501:22 — CTA 위 안내 2줄 14.5 #8E97A6 가운데 -->
    <TextView
        android:id="@+id/text_note"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:gravity="center"
        android:includeFontPadding="false"
        android:letterSpacing="-0.015"
        android:lineHeight="20sp"
        android:text="계정에 표시될 이름이에요\n나중에 내 정보에서 바꿀 수 있어요"
        android:textColor="@color/ink_muted"
        android:textSize="14.5sp" />

    <Button
        android:id="@+id/btn_next"
        style="@style/BigButton"
        android:layout_marginStart="24dp"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="24dp"
        android:text="이름 저장하고 계속" />
</LinearLayout>
