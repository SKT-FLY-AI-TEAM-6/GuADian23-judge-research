<?xml version="1.0" encoding="utf-8"?>
<!--
  10G · 보호 결과 (피그마 v2 · 1039:139).
  앱바(뒤로 44 · 제목 20 Bold) → 가운데 체크 56 → 제목 26 Bold → 시각 14 #8E97A6 →
  결과 카드(#F5F7FA r16 · 체크 22 + 15.5 Bold 세 줄) → 「서비스가 도운 과정」 타임라인 →
  안내 13 → CTA 56. v1의 화면 끝까지 닿던 파란 머리는 v2에서 사라졌다.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_screen"
    android:orientation="vertical"
    android:paddingTop="8dp"
    android:paddingBottom="24dp">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="44dp">

        <ImageView
            android:id="@+id/btn_back"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_marginStart="7dp"
            android:contentDescription="뒤로"
            android:src="@drawable/ic_back" />

        <TextView
            style="@style/AppBarTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_marginStart="52dp"
            android:text="보호 결과" />
    </FrameLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:scrollbars="none">

        <LinearLayout
            android:id="@+id/body"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="24dp"
            android:paddingEnd="24dp" />
    </ScrollView>

    <!-- CTA·저장 실패 안내: Kotlin에서 추가 (이미 확인한 기록이면 문구 다름) -->
    <LinearLayout
        android:id="@+id/footer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginTop="12dp"
        android:layout_marginEnd="24dp"
        android:orientation="vertical" />
</LinearLayout>
